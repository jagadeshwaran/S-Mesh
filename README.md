# S-Mesh
Has the details and code of smart mesh

Program:

- Python lists are used to store the data
- An upgrade - Sqlite is also used to store the data
- Mcast done to all the nodes for getting configured to same data and unique node ( unicast should be the better way )
